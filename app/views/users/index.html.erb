<p style="color: green"><%= notice %></p>

<h1>Users</h1>

<div id="users">
  <% @users.each do |user| %>
    <p>
      <%= link_to "Show this #{user.email}", profile_path(user.id) %>
    </p>
  <% end %>
</div>
